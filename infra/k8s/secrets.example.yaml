# ⚠️ DEPRECATED — Este arquivo e mantido apenas como referencia historica.
#
# A gestao de segredos do SIGIL agora e feita via HashiCorp Vault +
# External Secrets Operator (ESO), NAO mais via Secret estatico manual.
#
# Ver:
#   - infra/k8s/vault/vault-deployment.yaml      (Vault no cluster)
#   - infra/k8s/vault/external-secrets-operator.yaml (SecretStore)
#   - infra/k8s/vault/external-secret-backend.yaml   (ExternalSecret do backend)
#
# NUNCA versionar segredos reais neste ou em qualquer outro arquivo YAML.
apiVersion: v1
kind: Secret
metadata:
  name: sigil-backend-secrets-EXEMPLO-NAO-USAR
  namespace: sigil
type: Opaque
stringData:
  SECRET_KEY: "OBSOLETO-usar-vault-kv-put-secret/sigil/backend"
  POSTGRES_PASSWORD: "OBSOLETO-usar-vault"
  NEO4J_PASSWORD: "OBSOLETO-usar-vault"
  MINIO_SECRET_KEY: "OBSOLETO-usar-vault"
