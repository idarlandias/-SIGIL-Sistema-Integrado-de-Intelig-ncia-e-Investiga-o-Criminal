# NUNCA versionar secrets reais. Este é um template — gere o real com:
# kubectl create secret generic sigil-backend-secrets --from-env-file=.env -n sigil
apiVersion: v1
kind: Secret
metadata:
  name: sigil-backend-secrets
  namespace: sigil
type: Opaque
stringData:
  SECRET_KEY: "changeme-use-vault-em-producao"
  POSTGRES_PASSWORD: "changeme"
  NEO4J_PASSWORD: "changeme"
  MINIO_SECRET_KEY: "changeme"
# Em produção: substituir por integração com HashiCorp Vault via
# Vault Agent Injector ou External Secrets Operator, nunca hardcoded aqui.
